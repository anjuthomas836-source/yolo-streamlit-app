
# LOGIN PART (already added above)

if st.session_state.logged_in:

    st.sidebar.success("Logged in successfully")
    
    if st.sidebar.button("Logout"):
        st.session_state.logged_in = False
        st.rerun()

    # ---------------- MAIN APP START ----------------
    st.title("YOLO Object Detection App")